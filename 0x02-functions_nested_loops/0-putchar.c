#include <stdio.h>
/*
   *main - main function
   * return(0) - returns 0 if the program is true 
   */

int main()
{
	putchar(_);
	putchar(p);
        putchar(p);
        putchar(p);
        putchar(p);
	putchar(p);
        putchar(p);
        putchar(p);
        putchar(p);

	return (0)
}
